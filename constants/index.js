const CATEGORIES = {
	TOP: "top",
	STANDART: "standart",
};

const DEFAULT_PAGE = 1;
const DEFAULT_PER_PAGE = 4;

const SORT_FIELDS = ["price", "title"];

module.exports = {
	CATEGORIES,
	DEFAULT_PAGE,
	DEFAULT_PER_PAGE,
	SORT_FIELDS,
};
